[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Private service: no public domain. Hermes listens on :8642 internally.
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 8
numReplicas = 1
